//! Factorial and repeated-measures analysis of variance (0.17 — under
//! construction; see the module-level plan in the 0.17 track).

use crate::base::errors::SymplexError;

/// Placeholder so the module is reachable; replaced by the 0.17 track.
#[doc(hidden)]
pub fn __under_construction() -> Result<(), SymplexError> {
    Ok(())
}
