//! Tests written alongside the 0.26 changes (`tests/v25/*.rs`): numerical
//! evaluation that knows how many of its digits are right, and the
//! canonical and printed forms of radicals.

#[path = "v25/v25_evalf.rs"]
mod v25_evalf;
#[path = "v25/v25_radicals.rs"]
mod v25_radicals;
